import { listMatches } from "@/lib/services/football"; import { FixtureCard } from "@/components/site/fixture-card";
export const dynamic="force-dynamic";
export default async function MatchesPage(){let matches:any[]=[];try{matches=await listMatches()}catch{} return <div><section className="bg-black px-5 py-20 text-white lg:px-8"><div className="mx-auto max-w-7xl"><p className="text-xs font-black uppercase tracking-[.3em] text-red-500">Match centre</p><h1 className="mt-3 text-5xl font-black sm:text-7xl">Fixtures & results</h1><p className="mt-5 max-w-2xl text-white/60">Follow Solmart FC fixtures and results, with match details updated by the club.</p></div></section><section className="mx-auto max-w-7xl px-5 py-16 lg:px-8"><div className={`grid gap-6 ${matches.length === 1 ? "mx-auto w-full max-w-4xl" : "md:grid-cols-2 lg:grid-cols-3"}`}>{matches.map(m=><FixtureCard key={m.id} match={m}/>)}{!matches.length&&<p className="text-zinc-500">No fixtures are available yet.</p>}</div></section></div>}
