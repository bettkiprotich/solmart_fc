import { listPublishedNews } from "@/lib/services/content"; import { NewsCard } from "@/components/site/news-card";
export const dynamic="force-dynamic";
export default async function NewsPage(){let articles:any[]=[];try{articles=await listPublishedNews()}catch{} return <div><section className="bg-zinc-100 px-5 py-20 lg:px-8"><div className="mx-auto max-w-7xl"><p className="text-xs font-black uppercase tracking-[.3em] text-red-600">Newsroom</p><h1 className="mt-3 text-5xl font-black sm:text-7xl">Latest stories</h1><p className="mt-5 max-w-2xl text-zinc-600">Club news and updates will be published through the CMS.</p></div></section><section className="mx-auto max-w-7xl px-5 py-16 lg:px-8"><div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{articles.map(a=><NewsCard key={a.id} article={a}/>)}{!articles.length&&<p className="text-zinc-500">No published stories yet.</p>}</div></section></div>}
