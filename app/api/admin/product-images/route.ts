import { NextResponse } from "next/server"; import { z } from "zod"; import { prisma } from "@/lib/db/prisma"; import { requireRole } from "@/lib/auth/authorization"; import { assertSameOrigin } from "@/lib/http/request"; import { jsonError } from "@/lib/http/response"; import { removeUploadedFile } from "@/lib/services/uploads";
const schema=z.object({productId:z.string().cuid(),url:z.string().startsWith("/uploads/"),altText:z.string().min(1).max(200),sortOrder:z.coerce.number().int().min(0).default(0)});
export async function POST(request:Request){const e=assertSameOrigin(request);if(e)return e;const a=await requireRole("ADMIN");if(!a)return jsonError("Administrator access required.",403);const p=schema.safeParse(await request.json());if(!p.success)return jsonError("Invalid product image data.",422,p.error.flatten());try{return NextResponse.json({image:await prisma.productImage.create({data:p.data})},{status:201});}catch{return jsonError("Unable to save product image.",409);}}
export async function DELETE(request:Request){const e=assertSameOrigin(request);if(e)return e;const a=await requireRole("ADMIN");if(!a)return jsonError("Administrator access required.",403);const id=new URL(request.url).searchParams.get("id");if(!id)return jsonError("Image id is required.",422);try{const image=await prisma.productImage.findUnique({where:{id}});if(!image)return jsonError("Image not found.",404);await prisma.productImage.delete({where:{id}});await removeUploadedFile(image.url);return NextResponse.json({ok:true});}catch{return jsonError("Unable to remove product image.",404);}}
