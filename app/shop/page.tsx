import { listActiveProducts } from "@/lib/services/catalog"; import { ProductCard } from "@/components/site/product-card";
export const dynamic="force-dynamic";
export default async function ShopPage(){let products:any[]=[];try{products=await listActiveProducts()}catch{} return <div><section className="bg-zinc-950 px-5 py-20 text-white lg:px-8"><div className="mx-auto max-w-7xl"><p className="text-xs font-black uppercase tracking-[.3em] text-red-500">Official store</p><h1 className="mt-3 text-5xl font-black sm:text-7xl">Wear the crest.</h1><p className="mt-5 max-w-2xl text-white/60">Jerseys are the initial merchandise focus. Browse approved club products and use the secure checkout flow when ordering.</p></div></section><section className="mx-auto max-w-7xl px-5 py-16 lg:px-8"><div className="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">{products.map(p=><ProductCard key={p.id} product={p}/>)}{!products.length&&<p className="text-zinc-500">No merchandise is available yet.</p>}</div></section></div>}
